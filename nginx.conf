server {
    listen 80;
    server_name your-domain.com;

    location / {
        root /path/to/your/react/app/build;  # Set the path to your build folder
        try_files $uri /index.html;
    }
}
